package com.corejava.OOPs.Inheritance;

public class Inheritance 
{
	/* Inheritance :
	 * 
	 * Inheritance is a process where child class acquires the properties 
	 * of super class. 
	 * or
	 * Inheritance is a process where features of parent/super class are
	 * inherited in child/sub class. (MyClass1.java)
	 * 
	 * Inheritance can be achieved by using following keywords:
	 * extends and implements
	 * 
	 * Class always extends another class.
	 * 
	 * Class always implements interface.
	 * 
	 * IS-A relationship with Classes>
	 * SuperClass IS-A Object (because all classes extends java.lang.Object 
	 * by default)  (MyClass2.java)
	 * 
	 * Interface always extends another interface. 
	 * 
	 * Interface can extend more than one interface.
	 * 
	 * 
	 */

}
