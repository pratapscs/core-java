package com.corejava.OOPs.Varargs;

public class Varargs 
{
	/* Varargs method allows us to write the methods which can take 
	 * variable length of arguments in java.
	 * 
	 * Variable arguments as the name suggests can take variable i.e any 
	 * number of argument (may be 0, or 1, or 2, till any number) in java.
	 * 
	 * Varargs method /Variable arguments were introduced in Java 5.
	 * 
	 * syntax:
	 * static void myMethod(String... str){}
	 * Three dots dots signifies the varargs.
	 * 
	 * 
	 *  
	 */

}
