package com.corejava.InterviewPrograms;

public class SQLInterviewQuesions 
{
	/* 1.What is the difference between TRUNCATE between DELETE IN Oracle?
	 * TRUNCATE resets the HWM(High Water Mark) whereas DELETE doesn't. 
	 * DELETE command will log the data changes in the log file where as 
	 * the TRUNCATE will simply remove the data without it. 
	 * Data removed by DELETE command can be ROLLED BACK but not the data removed by TRUNCATE. 
	 * TRUNCATE is a DDL statement whereas DELETE is a DML statement.
	 * 
	 * 2.What Is SQL*Plus?
	 * SQL*Plus is an interactive and batch query tool that is installed with every 
	 * Oracle Database Server or Client installation. It has a command-line user interface, 
	 * a Windows Graphical User Interface (GUI) and the iSQL*Plus web-based user interface.
	 * 
	 * 3.
	 */

}
